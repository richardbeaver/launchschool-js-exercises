// Multiplying Two Numbers

// Create a function that takes two arguments, multiplies them together, and
// returns the result.

// Example:
console.log(multiply(5, 3) === 15); // logs true

//

/*
- Problem

inputs: two arguments
outputs: the result of multiplying the two inputs

- Algorithm

1. Return the result of multiplying the two inputs

*/

function multiply(num1, num2) {
  return num1 * num2;
}

// Would need to be defined before being called:
// const multiply = (num1, num2) => num1 * num2;
